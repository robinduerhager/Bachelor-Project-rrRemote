import { DArtist, DArtistProps, Email, EmailProps, UID } from './artist'
import { Artist } from './generated/graphql-types'

export {
  Artist,
  DArtist,
  DArtistProps,
  Email,
  EmailProps,
  UID
}